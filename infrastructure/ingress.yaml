apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: os-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: 'echo.dev2.openskool.dev'
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: echo
                port:
                  number: 80
    - host: 'hello.dev2.openskool.dev'
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: hello
                port:
                  number: 80
  tls:
    - hosts:
        - echo.dev2.openskool.dev
        - hello.dev2.openskool.dev
      secretName: os-tls
